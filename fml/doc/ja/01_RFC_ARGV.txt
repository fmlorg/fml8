			コマンドの使い方


『データを見るだけのもの』は、これはこれで別に構わないと思うのだけれど、
『操作をするもの』カテゴリってのがいくつかある。fmlhtmify は別としても、
こいつらは分離しないで makefml に統合するべきかな〜と思うところアリ。
ばらさないようにすれば admin コマンドや CGI でも使えるようになり得るしね。


＊ データを見るだけのもの

	fmladdr	 [-n]
	fmlalias [-n]
	fmldoc   モジュール名
	fmlconf  [-n] $ml

   うーむ？
	fmlerror $ml
	fmlerror $ml [アルゴリズム？]


＊ 何らかの操作をするもの

	makefml    $command $ml
	fml        $ml $command	(makefml と一緒)


＊ 何らかの操作をするもの (の特殊なもの)
	fmlhtmlify [-I DIR] $src_dir $dst_dir


＊ バラバラなので、makefml に統合するべきかな〜とおもうもの
	fmlsummary $command $ml
	fmlthread  $command $ml
	fmlspool   [options] $command $ml
	fmlsuper   $command $ml

		->

	* admin summary と user summary
	fml $ml summary			単なる表示 (user summary はこれだけ)
	fml $ml summary $subcommand

	fml $ml thread			単なる表示
	fml $ml thread  $subcommand

	fml $ml spool			単なる表示
	fml $ml spool	$subcommand

	fml $ml mailq			単なる表示
	fml $ml mailq	$subcommand


--------------------
$FML$
