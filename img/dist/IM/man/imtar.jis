			IMTAR マニュアル


機能:

	フォルダの中身を1つのメッセージにまとめる

オプション:

	--src=<folder>	まとめる元フォルダを指定する。
	--dst=<string>	まとめ先のファイルを指定する。
	--noharm=<sw>	on: 動作の内容を表示するだけで、実際のまとめは行
			なわない。
	--verbose=<sw>	on: 処理に関するさまざまな情報を表示する。
	--debug=<sw>	on: デバッグメッセージを表示する。
	--help=<sw>	on: ヘルプメッセージを表示する。
