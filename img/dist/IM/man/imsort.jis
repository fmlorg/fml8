			IMSORT マニュアル


機能:

	メッセージのソート

オプション:

	--src=<folder>	ソートを行なうフォルダを指定する。
	--field=<string> ソートを行う対象のフィールドを指定する。
        --mode=<string> ソートを行うモード (date, num, text, ml のいずれか)
			を指定する。
	--noharm=<sw>	on: 動作の内容を表示するだけで、実際のソートは行
			なわない。
	--verbose=<sw>	on: 処理に関するさまざまな情報を表示する。
	--debug=<sw>	on: デバッグメッセージを表示する。
	--help=<sw>	on: ヘルプメッセージを表示する。
